# Hook Registry System Requirements - V3.6.9

# Core dependencies
flask>=2.3.0
flask-cors>=4.0.0
pyyaml>=6.0
psutil>=5.9.0
watchdog>=3.0.0

# Optional dependencies for enhanced functionality
numpy>=1.24.0  # For performance calculations
pandas>=2.0.0  # For data analysis in monitoring
requests>=2.31.0  # For HTTP-based integrations

# Development dependencies (optional)
pytest>=7.4.0  # For testing
pytest-asyncio>=0.21.0  # For async testing
black>=23.0.0  # Code formatting
pylint>=2.17.0  # Code linting

# Optional audio dependencies (if using audio hooks)
pygame>=2.5.0  # For audio playback on multiple platforms

# Optional async dependencies
aiohttp>=3.8.0  # For async HTTP operations
asyncio-mqtt>=0.13.0  # For MQTT integration (if needed)

# Database dependencies (optional, for persistent storage)
sqlalchemy>=2.0.0  # ORM for database operations
sqlite3  # Built-in, but listed for clarity

# Monitoring and metrics (optional)
prometheus-client>=0.17.0  # For Prometheus metrics
grafana-api>=1.0.3  # For Grafana dashboard integration

# Web UI dependencies (optional)
fastapi>=0.103.0  # Alternative to Flask for async APIs
uvicorn>=0.23.0  # ASGI server for FastAPI
jinja2>=3.1.0  # Template engine for web UI

# Security dependencies
cryptography>=41.0.0  # For secure communications
pyjwt>=2.8.0  # For JWT token handling

# Configuration and validation
pydantic>=2.3.0  # For data validation
jsonschema>=4.19.0  # For JSON schema validation

# Logging and debugging
colorlog>=6.7.0  # Colored logging output
rich>=13.5.0  # Rich terminal formatting

# Version constraints for stability
setuptools>=68.0.0
wheel>=0.41.0
pip>=23.2.0